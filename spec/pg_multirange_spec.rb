# frozen_string_literal: true

RSpec.describe PgMultirange do
  it "has a version number" do
    expect(PgMultirange::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
